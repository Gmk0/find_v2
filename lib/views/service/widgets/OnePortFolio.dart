import 'package:flutter/material.dart';

class OnePortFolio extends StatelessWidget {
  const OnePortFolio({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
