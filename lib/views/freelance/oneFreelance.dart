import 'package:flutter/material.dart';

class OneFreelance extends StatefulWidget {
  const OneFreelance({super.key});

  @override
  State<OneFreelance> createState() => _OneFreelanceState();
}

class _OneFreelanceState extends State<OneFreelance> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
